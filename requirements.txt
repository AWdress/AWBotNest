# ============================================================
# AWBotNest 依赖清单（版本已锁定，确保可复现构建）
# 版本对应 v2.1.7 实际运行环境（Python 3.13）
# ============================================================

# --- Telegram 框架 ---
kurigram==2.2.22
# 加密加速（kurigram 的可选 fast extra）。原 tgcrypto==1.2.5 无 Python 3.13 的
# Linux 预编译 wheel，会在无编译器的 slim 镜像里从源码编译失败；改用维护中的
# 同名 drop-in 分叉 TgCrypto-pyrofork（提供 cp313 Linux wheel，import 名仍是 tgcrypto）。
TgCrypto-pyrofork==1.2.8; sys_platform != 'win32'

# --- 异步事件循环加速（非 Windows）---
uvloop==0.21.0; sys_platform != 'win32'

# --- 核心架构依赖 ---
dependency-injector==4.49.0
pydantic==2.13.3
pydantic-settings==2.14.0

# --- OCR / 图像 ---
ddddocr==1.6.1
Pillow==12.2.0
imgkit==1.2.3
# 注意：imgkit 依赖系统二进制 wkhtmltoimage / wkhtmltopdf，
# 需通过系统包管理器或官方安装包单独安装（不是 pip 包）。

# --- 任务调度 ---
APScheduler==3.11.2

# --- 网络 / HTTP ---
aiohttp==3.13.5
httpx==0.28.1
requests==2.33.1

# --- 解析 ---
beautifulsoup4==4.14.3
lxml==6.1.0

# --- 时间 ---
pytz==2026.1.post1

# --- AI ---
openai==2.32.0

# --- 加密 ---
cryptography==46.0.7
pyjwt==2.12.1

# --- 文本 / Emoji ---
emoji==2.15.0
pypinyin==0.55.0
pygments==2.20.0

# --- 存储 ---
sqlitedict==2.1.0
sqlalchemy==2.0.49
aiosqlite==0.22.1
aiomysql==0.3.2
asyncpg==0.31.0

# --- 配置 ---
toml==0.10.2

# --- 数据处理 / 导出 ---
pandas==3.0.2
openpyxl==3.1.5
numpy==2.4.4

# --- 文件 IO ---
aiofiles==25.1.0

# --- Web UI ---
fastapi==0.136.1
uvicorn==0.46.0
pyngrok==8.0.0
python-multipart==0.0.32
websockets==15.0.1
