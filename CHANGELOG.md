# 更新日志

## v2.0.0.0_dev

- Docker 恢复 V1 的 Xvfb 直接启动方式；控制台日志明确输出到 stdout，网页与账号、插件初始化并行，避免网络等待阻塞管理页面。构建时保留 Xvfb 启动验证。

- 全新 Telethon 插件运行时与 Vue 管理界面。
- 支持插件市场、Vue 模块联邦、开放 API、Cookie 服务和 AI 服务。
- 支持 Windows、Linux 与 Docker 部署。
- 新增透明主题、深色主题与二次元随机背景 API，支持自定义背景。
- 用户菜单新增主题子菜单与定制主题对话框，采用毛玻璃视觉效果。
- 优化背景遮罩、文字对比度与菜单层级，避免亮色背景干扰内容阅读。
- 修复主题菜单状态残留、背景加载闪屏及应用启动时的交互问题。
- 修复备份压缩阻塞平台接口的问题。
- 修复 V1 迁移时插件排序可能丢失、迁移阻塞和压缩包路径校验不足的问题。
- 新增 Docker 直升自动迁移及迁移完成后的重启提示。
- 全面修复配置文件损坏时被默认配置覆盖、关闭全部插件重启后被重新启用、AI 模型别名回退错误、账号范围未生效等逻辑问题。
- 修复插件更新下载失败误删旧插件、并发安装互相覆盖、V1 迁移运行中覆盖配置、SQLite WAL 备份不完整及挂载目录恢复失败。
- 加固备份和迁移压缩包的路径、符号链接、大小及根节点校验；CookieCloud 无效响应不再清空 Cookie，清空 Cookie 同步快照。
- 修复日志并发读取、活动统计坏数据、非 ASCII 令牌校验、PWA 跨站图片无限缓存和退出登录后的过期状态写回。
