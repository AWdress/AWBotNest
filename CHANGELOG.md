# 更新日志

<!-- 版本日志使用中文白话文，说明用户能做什么，不记录内部实现、构建命令或排查过程。新增功能写入当前版本；当前版本开发期间自身问题的修复不追加日志，只有修复上一版本遗留问题时才记录修复条目。 -->

这里记录每个版本给用户带来的新功能、改进和问题修复。

## v2.0.0.6

这一版修复了 V1/V2 插件共存时的版本识别和热度统计问题。

- 修复同一插件的 V1 单文件与 V2 目录形态共存时，市场缓存读取错误版本、反复提示更新的问题。
- 修复安装一种插件形态时误删除另一种形态的问题，V1 与 V2 可以按各自清单正常使用。
- 修复插件轮询或重复上报导致热度持续虚增的问题；同一安装周期、同一插件、同一版本只计一次。
- 修复卸载后重新安装不重新计入热度的问题；V1/V2 使用相同插件 ID 时，热度仍统一汇总。

## v2.0.0.5

这一版修复了插件热度会一直增长的统计错误，并让 CookieCloud 配置保存即生效。

- 修复插件同时残留旧单文件和新版目录时，系统一直读取旧版本号、反复判定有新版本的问题；现在只保留正在使用的形态。
- 修复插件热度把模板文件和备份文件也算成安装，导致热度虚高的问题。
- CookieCloud 不再需要单独打开开关，生成凭据或填好远程信息并保存后直接可用。
- 旧配置里残留的开关字段会在下次保存时自动清理，不影响已经配置好的凭据和远程同步。

## v2.0.0.4

这一版改进了 Telegram 账号的网络恢复能力，并让 Bot 通知不再依赖用户账号保持在线。

- 网络中断时间较长时，Telegram 账号会持续尝试重新连接；网络恢复后复用已有会话自动上线。
- 首个用户账号登录成功后会保存 Bot 默认接收目标，用户账号离线或刚恢复时不影响 Bot 通知。
- Bot 增强消息接口不可用时会自动改用 Telegram 标准消息接口，无需重启系统恢复通知。

## v2.0.0.3

这一版完善了开放接口和手机端 API 文档体验。

- 开放 API 支持上传 Python 插件文件，校验通过后保持停用，不会自动执行；校验失败会恢复原文件。
- API 文档补充插件上传接口和日志读取接口说明，便于自动化程序接入。
- 修复手机端 API 文档顶部被状态栏遮挡的问题，并适配安全区间距。
- 修复插件配置和系统设置中的下拉选项文字与背景对比不足、选项难以辨认的问题。

## v2.0.0.2

这一版新增平台级浏览器仿真管理，让插件使用 CloakBrowser 时统一遵循平台设置和网络代理。

**浏览器仿真：**

- 系统设置可以选择 Chromium 或 CloakBrowser，默认继续使用兼容现有插件的 Chromium。
- 选择 CloakBrowser 后，平台会把兼容组件自动安装到持久化插件依赖目录，不打包进镜像。
- 可以保存并单独启用免费 Key；启用后，全平台通过单会话队列协调多个插件调用，关闭后仍使用原有免费内核。
- 设置页可以查看组件版本、浏览器会话和排队状态，并手动检查更新。
- 平台每天自动检查一次兼容组件版本；未选择 CloakBrowser、关闭免费 Key 或未填写 Key 时会静默跳过且不联网。

**CloakBrowser 代理：**

- 插件未单独指定代理时，CloakBrowser 会自动使用平台当前配置的代理。
- 浏览器内核、许可证和 GeoIP 数据下载会使用本次浏览器启动选择的代理，代理失败时不会静默改为直连。
- 插件仍可使用独立代理或明确选择直连；修改平台代理后，后续浏览器调用会自动使用新配置。
- 代理凭据不会写入全局代理环境变量，避免无关插件读取。

## v2.0.0.1

这一版新增平台 AI 调用日志与用量统计，改进模型较多时的模型库管理体验，并提供轻量配置迁移。

**AI 调用日志：**

- 文字、视觉和生图模型被调用时，会记录调用来源、模型、服务商、耗时及成功或失败状态。
- 服务商返回用量信息时，会显示输入、输出和总 Token 数；使用备用模型时也会记录切换过程。
- 日志不会记录提示词、图片内容或 API Key，避免敏感信息泄露。
- AI 服务商可以自动识别或固定使用 Chat Completions、Responses、Anthropic Messages 接口格式。
- AI 调用失败会区分认证、权限、模型、接口路径、限流、网络、上游和响应格式问题，方便直接定位原因。

**模型库管理：**

- 模型库改为紧凑列表，可以按名称、别名、服务商、模型能力和启用状态查找模型。
- 模型较多时可以分页浏览，点击需要修改的模型再展开编辑，减少设置页面长度。
- 已读取的模型列表默认收起，并优化手机端的筛选和编辑布局。

**AI 用量统计：**

- AI 服务页可以查看总调用、成功、失败、进行中数量，以及输入、输出和总 Token 用量。
- 用量数据会自动刷新并在平台重启后保留；主模型切换备用模型时，仍按一次完整调用统计。
- 可以查看最近调用的模型、协议、来源、耗时和 Token，并按插件汇总调用次数、成功失败和平均耗时。

**配置迁移：**

- 导出功能改为仅生成系统设置和插件设置的轻量配置包，不再打包账号会话、Cookie、日志、插件程序等运行数据。
- 导入时只替换系统设置和插件设置，支持从旧版备份中只提取配置，并限制配置包大小以避免大文件占用过多资源。
- 导入前会预览系统设置和插件设置的新增、修改及移除数量，确认时还会检查配置包和当前设置是否发生变化。

**运行与开发接口：**

- 新增独立的存活和就绪检查，容器会在平台配置、调度器和插件运行环境真正就绪后再报告健康。
- 提供与当前后端接口同步的 OpenAPI 描述和前端类型，降低扩展管理界面时的接口字段错误。
- 开放 API 文档改为中文引导页面，只展示稳定的 `/api/v1` 接口，并提供授权步骤、调用示例、接口分组和参数说明。

## v2.0.0.0

这一版带来重新设计的管理界面，增加透明主题和自定义背景。

**管理界面：**

- 重新设计运行概览、插件管理和用户菜单，集中查看平台运行情况、管理插件和调整个人设置。
- 系统设置支持配置 GitHub Token，提高插件仓库查询、自动发现和更新的 API 请求配额。

**主题与背景：**

- 可以在用户菜单中选择透明主题或深色主题，透明主题采用毛玻璃效果。
- 点击“定制主题”打开设置对话框，可以填写背景图片地址或随机图片接口地址。
- 提供默认二次元背景，登录页也支持显示背景图片。
