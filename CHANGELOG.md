# 更新日志

<!-- 项目文字要求：版本日志、提交说明和推送说明统一使用中文白话文，说明用户能做什么、解决了什么问题；不要记录构建命令、框架实现、内部字段名或排查过程。必须出现的专业名称要先用一句白话解释。 -->

这里记录每个版本给用户带来的新功能、改进和问题修复。

## v1.1.3.6

这一版重新整理了控制台常用操作，增加通知中心和管理员头像，也让日志、健康检查和定时任务查看更方便。

**界面改进：**
- 页面右上角增加快捷入口，可以直接打开实时日志、网络测试、系统健康检查和定时服务。
- 增加通知中心，平台和插件发出的通知可以在网页里统一查看、标记已读或清空，重启后记录仍会保留。
- 管理员头像可以直接修改，并保存到数据目录，更新容器后不会丢失。
- 左下角改为紧凑的信息卡，集中显示版本、连接状态和项目仓库，不再显示重复的重启和退出按钮。
- 插件配置窗口标题旁会显示插件自己的图标，识别插件更直观。

**使用改进：**
- 所有运行日志都改为最新记录排在最前面，暂停后继续查看时会自动补回遗漏的记录。
- 定时服务可以查看任务状态并手动执行，正在运行的任务不会被重复触发。
- 网络测试只显示服务名称和结果，不会把后台配置的服务地址带到网页中。
- 系统健康检查会显示账号、Bot、插件、数据库、通知、AI 和定时服务等运行情况。

**修复问题：**
- 修复右上角菜单被页面内容遮住，导致快捷入口看得到但点不开的问题。
- 修复相同时间、相同内容的日志可能被误认为重复记录的问题。
- 修复头像文件格式不正确时仍可能被保存的问题。
- 修复通知内容可能带出常见密钥、令牌或地址中账号密码的问题。

## v1.1.3.5

这一版修复了插件停用后仍可能继续处理消息的问题，并防止平台被重复启动。

**修复问题：**
- 停用插件后会立即停止接收新消息，正在等待 AI 或网络返回的旧任务也会停止，不会在关闭后继续回复。
- 同一套平台不能再被 `uv`、`python`、`systemd` 或多个容器重复启动，避免出现网页里已经停用、后台却仍在运行的情况。
- Web 端口被占用时，本次启动会直接停止并给出明确提示，不再留下没有网页界面的插件进程。
- 无论从哪个目录启动平台，都会使用项目目录里的同一套配置和数据，避免 systemd 与手动启动读取不同状态。

## v1.1.3.4

这一版修复了代理和数据库连接检查，并让平台代理在各项平台功能中保持一致。

**修复问题：**
- 修复测试代理和测试数据库时提示“Not Found”的问题，网页后台现在可以正常检查连接。
- 补全 SOCKS 代理和 PostgreSQL 所需组件，选择这些类型后不会再因为平台缺少组件而失败。
- 修复带账号密码的代理无法用于 Telegram 连接的问题，插件仓库自动发现和平台浏览器也会正确使用平台代理。
- 网页代理关闭后会恢复 Docker 原有的代理设置，不会再把 Compose 中配置的代理清掉。
- 代理启动日志不再显示账号和密码，避免敏感信息写入日志。

## v1.1.3.3

这一版修复了系统设置无法保存的问题，并进一步明确了后台设置的访问权限。

**修复问题：**
- 修复系统设置保存时提示“Method Not Allowed”的问题，各项设置现在可以正常保存。
- 平台设置、AI 设置和通知渠道设置仍然只允许登录后的后台管理员修改。
- 第三方 API Key 不能读取或修改平台系统设置，只能使用平台已经开放的第三方接口。

## v1.1.3.2

这一版完善了平台 AI 设置，生图等待更稳定，也可以由平台为不同插件指定模型。

**AI 服务改进：**
- 文字和图片识别继续使用普通请求超时，生图改为独立超时，默认等待 300 秒，减少生图时间较长导致的失败。
- 系统设置里可以分别调整普通 AI 请求和生图请求的等待时间。
- 可以为每个插件分别指定文字、图片识别和生图模型，插件本身不需要修改或更新。
- 插件 AI 设置现在只显示真正使用平台 AI 的插件，普通插件不再占用列表位置。
- 没有单独指定模型的插件会继续跟随平台默认设置，不影响现有插件使用。
- 指定的模型暂时不可用时会自动尝试平台默认模型，避免插件直接停止工作。

## v1.1.3.1

这一版降低了平台在消息较多时的 CPU 占用，让多个插件同时启用时运行更轻、更稳定。

**性能改进：**
- 修复每收到一条消息都会重复读取和分析插件文件的问题，插件配置现在会直接使用已经读取好的信息。
- 插件安装、更新或重载后仍会自动读取最新内容，不影响插件正常更新。
- 群组消息较多、同时启用多个插件时，平台不会再因为重复处理插件信息而长期占满一个 CPU 核心。

## v1.1.3.0

这一版全面清理了代码，修复了 20 个潜在问题，删除了 1600 多行不再使用的旧代码，让平台更安全、更稳定。

**安全性改进：**
- 修复上传文件时可能被恶意路径穿越到系统其他目录的问题，现在文件只能保存到插件自己的目录。
- 修复恢复备份时可能被压缩炸弹（解压后体积暴增的恶意压缩包）攻击的问题，现在会检查解压后的总大小。
- 修复下载依赖时没有校验完整性的问题，损坏或被篡改的文件现在会被拒绝。

**稳定性改进：**
- 修复通知发送失败后不会重试的问题，现在会自动重试 3 次。
- 修复浏览器启动和下载依赖包超时设置不合理的问题，现在会给足够的时间。
- 修复多个地方的异常处理不够精准、错误提示不够清楚的问题。

**代码清理：**
- 删除了 8 个不再使用的文件（重复的定时任务、旧的工具、废弃的代码）。
- 清理了从旧项目 AWBotHub 遗留下来的配置字段、函数和群组验证机制。
- 修复日志文件命名（原来的 `logs/Mytgbot.log` 改为 `logs/app.log`）。
- 删除了 1600 多行死代码，整个平台更干净。

**其他改进：**
- 所有改动都通过了 51 个自动化测试，确保不会影响现有功能。
- 向后兼容，旧插件可以继续正常工作。

**迁移说明：**
- 如果之前使用了定时任务版本的"自动回复"、"自动换头像"或"自动改名"，请在插件管理中启用对应的插件版本。
- 日志文件路径变更为 `logs/app.log`，旧的 `logs/Mytgbot.log` 可以手动删除。

## v1.1.2.7

这一版重新整理了插件配置里的通知渠道和应用账号选择，让配置窗口更紧凑，也修复了插件市场筛选菜单的显示和关闭问题。

**界面改进：**
- 通知渠道和应用账号改为同一行的紧凑多选框，减少配置窗口占用的空间。
- 选择框收起时会显示当前选择结果，展开后可以直接多选，改动仍会立即保存。
- 手机端会自动改为上下排列，避免选择框过窄。

**修复问题：**
- 修复插件市场筛选菜单背景透明的问题，内容不会再和后面的插件卡片混在一起。
- 点击筛选菜单以外的位置后，菜单现在会自动关闭。
- 修复通知渠道和应用账号的下拉菜单被配置窗口裁切或撑高窗口的问题；菜单现在会浮在窗口上层，并根据可用空间自动向上或向下展开。
- 修复进入“通知推送”页面后，插件路由没有正常读取或保存后没有及时刷新的问题。
- 通知渠道、推送路由和插件配置现在会继续保持同步，从任意一处修改都能看到最新结果。
- 重复选择的通知渠道会自动合并，避免同一条通知被重复发送。
- 修复系统设置里通知渠道无法正常保存的问题；编辑、新增、启停和删除渠道现在都能立即生效，新的 Bot 配置连接失败时也会保留原来的可用配置。

## v1.1.2.6

这一版把 AI 能力放进平台。插件需要处理文字、识别图片或生成图片时，可以直接使用平台统一配置的模型，不必各自重复填写服务地址和密钥。

**新增功能：**
- 系统设置新增“AI 服务”，可以添加多个兼容 OpenAI 接口的服务。
- 可以读取服务商提供的模型列表，也可以手动填写模型名称。
- 可以添加多个文字、图片识别和生图模型，并为每个模型设置方便插件使用的别名。
- 插件可以查看自己可用的模型并按需要自主选择；没有选择时使用平台设置的默认模型。
- 文字、图片识别和生图能力都可以分别设置默认模型和备用模型。
- 可以单独控制每个插件是否允许使用文字、图片识别或生图能力。

**使用改进：**
- AI 服务地址和密钥只需在平台配置一次，插件不会接触密钥。
- 平台 AI 默认可以直接调用，不再显示没有必要的总开关；需要限制时可关闭单个服务、模型或插件权限。
- 读取服务商模型后会直接显示可选列表，支持搜索、多选并加入模型库，加入后即可设置默认模型和备用模型。
- 模型搜索现在只按模型名称过滤，不会再因为服务商名称相同而把全部模型都显示出来。
- 手动选择真实模型时，下拉列表会固定显示在输入框正下方，不再跑到页面右侧。
- 当前模型不可用时，可以自动尝试备用模型。
- 图片识别支持插件下载到本地的图片，生成的图片会自动保存到插件自己的目录。
- 平台 AI 只供插件调用，不会增加聊天命令，也不会主动监听或回复消息。

**修复问题：**
- 修复网页上的“重启平台”按钮无法使用的问题，现在 Docker 部署和直接运行都可以从控制台重新启动。
- 默认模型所在的服务被停用、地址错误或无法正常使用时，备用模型现在可以正常接管。
- 插件只会看到当前真正可用的模型，不再显示已停用服务下的模型。
- 手动填写了不正确的超时或调用数量后，平台会自动恢复为安全数值，不会导致 AI 设置无法打开。
- 生成图片的下载地址发生跳转时会逐次检查，避免访问不安全的地址；图片也会按真实格式保存。
- AI 调用返回空内容或无效图片时会正确记为失败，页面上的调用统计更加准确。
- 服务地址填写错误时会直接给出清楚提示，插件看到的默认模型标记也会与所查询的能力一致。

## v1.1.2.5

这一版重新整理了插件配置窗口，账号和通知设置更集中，也更容易查看。

**界面改进：**
- “应用账号”已经移到插件的“配置”窗口中，不再单独打开一个弹窗。
- 三点菜单中不再重复显示“应用账号”，需要调整时直接打开插件配置即可。
- 通知渠道改为横向排列，渠道较多时会自动换行，不再占用过多纵向空间。
- 手机或窄屏设备会自动改为单列显示，避免内容被挤压。

**使用改进：**
- 修改插件的应用账号后会立即保存，不需要再打开单独窗口确认。
- 指定账号模式下会保留至少一个账号，保存失败时也会恢复原来的选择，避免页面显示与实际设置不一致。

## v1.1.2.4

这一版没有新增功能，主要在后台做了一次「大扫除」。

**改进：**
- 清理了一批早期版本遗留下来、如今已经用不上的内部代码。这些老代码早就被更完善的插件取代，留着只是占地方。
- 瘦身之后，平台运行更轻便，后续维护和更新也会更顺畅。

**适用场景：** 这次更新不影响你正在使用的任何功能，直接更新即可，无需做任何额外操作。

## v1.1.2.3

这一版集中修复了一批影响日常使用的稳定性和安全问题，建议更新。

**修复问题：**
- 修复了登录账号时偶尔卡住、后台连接没有正常释放的问题。
- 修复了多人同时点「上线」时，会把别人已经连上的账号顶掉的问题。
- 修复了刷新页面就能绕过「首次使用必须修改默认密码」的问题。
- 修复了快速连续点开两个插件的「配置」时，设置内容互相串号的问题。
- 修复了插件日志弹窗重复打开会泄漏连接、越开越卡的问题。
- 修复了平台重启等待期间切走页面、仍会被强制刷新的问题。
- 修复了异常退出时可能把账号密码文件写坏、导致密码被退回默认值的问题。
- 修复了网络拥堵时自动重试会拖慢其他正常请求的问题。
- 修复了部分设置改了不生效、状态文件偶尔写坏的问题。

**安全加固：**
- 上传插件增加了大小限制（单个文件不超过 5MB），避免超大文件把磁盘占满。

**适用场景：** 如果你遇到过账号莫名掉线、登录卡住，或担心默认密码被绕过，更新到这个版本即可解决。

## v1.1.2.2

这一版修复了多个 API 接口的错误，并加强了系统安全性。

**修复问题：**
- 修复了插件列表接口偶尔返回错误的问题（原因：调用了不存在的方法）。
- 修复了查看插件源码时可能出错的问题（原因：路径处理不当）。
- 修复了发送消息接口超时卡死的问题，现在会在 10 秒后返回明确错误提示。
- 改进了错误提示信息，不再显示技术细节，而是告诉你"群组 ID 不存在"等清晰的原因。

**安全加固：**
- 为了防止恶意攻击，关闭了 5 个可能危害系统安全的接口：
  - 不能通过接口远程修改系统配置（API ID、代理、数据库等敏感信息）
  - 不能通过接口远程重启平台
  - 不能通过接口修改插件源代码（这相当于让别人远程执行代码）
  - 第三方开放 API 不能测试代理和数据库连接（网页后台管理员仍可测试）
- 这些操作现在只能通过网页界面或直接编辑配置文件完成。

**测试结果：**
- 所有 19 个开放平台 API 接口测试通过，工作正常。

**适用场景：** 如果你在用 AI 工具或第三方系统调用平台接口时遇到错误，更新到这个版本可以解决问题。

## v1.1.2.1

这一版优化了插件开发文档，让插件开发者更容易找到合适的配置控件。

**改进：**
- 在插件开发指南中新增字段类型速查表，让开发者一眼看到所有可用的配置控件类型。
- 新增推荐排版规范，说明如何让功能开关整齐地排在配置界面最上方（参考专业界面设计）。
- 强调滑块控件（slider）的使用场景：延迟时间、音量大小、透明度、百分比等有范围的数值调节。

**适用场景：** 插件开发者编写配置界面时，能更快找到合适的控件类型，做出更专业的用户体验。

## v1.1.2.0

这一版开放了平台 API，让其他系统、AI 工具、第三方平台可以通过接口调用平台功能。

**新功能：**
- 新增开放平台 API（`/api/v1/`），支持用 API Key 验证身份。
- 在系统设置中可以生成 API_KEY，用于第三方工具调用平台接口。
- 可以通过 API 管理插件：列出、查看详情、读取/修改源代码、启用/停用/重载。
- 可以通过 API 修改插件配置、读写插件存储的数据（kv 键值对）。
- 可以通过 API 发送消息到指定群组或用户（用 bot 或 user 账号发送）。
- 可以通过 API 查询群组信息、账号状态、平台日志、插件日志。
- 可以通过 API 查看平台运行状态（版本、连接状态、插件数量等）。
- 适用场景：AI 辅助插件开发（如 Hermes、OpenClaw）、自动化运维、第三方系统集成。

**接口列表：**
- 插件管理：`GET/POST/PUT /api/v1/plugins/...`
- 插件配置：`GET/PUT /api/v1/plugins/{id}/config`
- 插件数据：`GET/PUT/DELETE /api/v1/plugins/{id}/kv/{key}`
- 消息发送：`POST /api/v1/messages/send`
- 会话查询：`GET /api/v1/chats/{chat_id}`
- 账号状态：`GET /api/v1/accounts`
- 日志查询：`GET /api/v1/logs`、`GET /api/v1/logs/plugins/{id}`
- 平台状态：`GET /api/v1/status`

## v1.1.1.3

这一版新增了通过群组 ID 直接获取群组名称的 API，方便插件开发。

**新功能：**
- 新增 `/api/chats/{chat_id}` 接口，可以通过群组/频道/私聊的 ID 直接获取名称和类型。
- 插件不再需要拉取全部对话列表来匹配名称，可以直接查询单个 ID。
- 支持数字 ID（如 `-1001234567890`）和用户名（如 `@username`）两种格式。
- 可以指定使用哪个账号来查询，未指定则使用首个已连接的用户账号。

## v1.1.1.2

这一版新增了自动发现第三方插件仓库的功能。

- 平台每天凌晨0点会自动搜索 GitHub 上所有名为 AWBotNest-Plugins 的仓库。
- 自动检查这些仓库是否包含插件清单（manifest.json），有的话会自动添加到插件市场。
- 同时搜索 fork 了官方仓库的项目，让更多开发者的插件能被自动发现。
- 已经手动添加过的仓库不会重复添加，官方仓库也会自动跳过。
- 发现并添加新仓库后会在日志中记录，方便查看。

## v1.1.1.1

这一版修复了插件自动更新不完整的问题，让所有来自插件市场的插件都能正常自动更新。

- 修复预装插件和第三方仓库插件不会自动更新的问题。
- 平台现在会读取本地插件的实际版本，并与远程版本对比，有更新就会自动下载。
- 首次轮询时如果发现本地版本低于远程版本，会立即更新插件。
- 手动上传的本地插件仍然受到保护，不会被插件市场的同名插件覆盖。

## v1.1.1.0

这一版优化了通知消息的格式兼容性，让企业微信、Bark 等渠道也能正确显示插件发送的富文本通知。

- 插件通知中的加粗、斜体、链接等格式现在可以在企业微信和 Bark 中正常显示，不再显示为 HTML 标签。
- 企业微信通知优先使用 Markdown 格式发送，服务端不支持时会自动改用纯文本，并保留链接地址。
- Bark 通知改用 JSON 接口发送，避免通知内容中的链接、斜杠或特殊字符破坏推送地址。
- 通知内容超出渠道字节限制时会自动截断并标记，避免整条消息被拒收。
- 修复企业微信通知中接收人参数为空时显示为 "None" 的问题，现在会正确使用默认值 "@all"。

## v1.1.0.13

这一版加快了通知渠道的保存速度，点击确认后不用再一直等着弹窗关闭。

- 点击确认后编辑窗口会立即关闭，保存和同步会继续在后台完成。
- 保存失败时会恢复修改前的内容并重新打开窗口，避免配置丢失。
- 修改 Bark 或企业微信渠道时，不再重复连接无关的 Telegram Bot。
- 没有变化的日志清理和插件仓库任务不会重复启动，减少保存等待。
- 保存完成后不再重复读取同一份设置，渠道和插件路由仍会保持同步。

## v1.1.0.12

这一版修复了插件市场安装和更新不稳定的问题，大型插件和文件较多的插件也能更顺利地处理。

- 修复文件夹插件安装或更新时可能提示没有可下载文件的问题。
- 减少读取插件仓库时的重复请求，避免短时间内请求过多导致安装失败。
- 加快多文件插件的下载速度，安装大型插件时不再逐个文件长时间等待。
- 安装或更新完成后会立即刷新插件信息，避免页面继续显示旧版本或旧状态。
- 插件文件已经更新但重新加载失败时会明确提示，不再误报为运行成功。
- GitHub 暂时限制请求时会显示容易理解的提示，方便稍后重试。

## v1.1.0.11

这一版优化了网页端的加载速度，首次打开和切换页面会更轻快。

- 首次进入控制台时只加载当前页面，插件、设置、日志和账号页面会在打开时再加载。
- 插件页面会先显示本地插件，插件市场数据稍后加载，减少进入页面时的等待。
- 合并了短时间内重复的状态请求，避免同一份数据连续读取多次。
- 页面资源会在更新后安全缓存，再次打开时可以更快显示。
- 页面入口和实时数据仍会读取最新内容，不会因为缓存看不到新版本或新状态。
- 修复旧加载规则导致插件页面资源被提前下载的问题。
- 只缓存正常返回的页面资源，网络异常时不会把错误内容长期保留下来。
- 加快了系统设置的打开速度，减少进入维护、通知等设置时的等待。
- 加快了首页状态和插件信息的读取速度，页面来回切换时响应更快。
- 插件新增、删除、更新或启停后仍会立即显示最新状态，不会读到旧数据。

## v1.1.0.10

这一版重点修复通知渠道、插件和推送路由之间不同步的问题，让通知设置更稳定，也更容易理解。

- 插件配置、推送路由和通知渠道现在会互相同步，在任意一处修改后，另外两处都会自动更新。
- 同一浏览器的不同页面和标签页也会同步通知设置，不需要手动刷新。
- 同一个插件可以同时使用多个通知渠道，其中一个发送失败不会影响其他渠道。
- 新增、修改、停用或删除通知渠道后会立即生效，不需要重启平台。
- 停用或删除渠道时，会自动从插件路由中移除这个渠道，同时保留插件已经选择的其他渠道。
- “跟随默认渠道”和“明确选择当前默认渠道”现在会分别显示，不会再误删多渠道选择。
- 企业微信或 Bark 设为默认渠道时，只会显示一个正确的默认渠道。
- 机器人类插件只能选择适用的 Telegram 渠道，三个设置入口的可选范围保持一致。
- 所有通知渠道都不可用时，会尝试把消息发送到在线账号的收藏夹，减少重要通知丢失。
- 通知渠道中的 Token、密钥等敏感内容不再直接显示在页面接口中。
- 修复通知渠道保存顺序、连续点击和旧配置迁移可能造成的保存失败或配置丢失问题。

## v1.1.0.9

这一版大幅优化了通知系统，让你可以更灵活地控制每个插件的通知方式，还能多渠道同时推送。

**通知渠道全面升级**
- 每个通知渠道现在可以选择接收哪些插件的通知，不用全部插件都发到同一个地方。
- 支持一键"全部"选择，也可以精细控制每个插件。
- 企业微信和 Bark 渠道会自动过滤掉需要 Bot 交互功能的插件（比如转发机器人），只显示纯通知类插件，避免误配置。
- Telegram 渠道支持所有插件，包括需要 Bot 功能的。

**多渠道同时通知**
- 同一个插件可以同时推送到多个通知渠道（比如 Telegram + 企业微信 + Bark），重要消息不会错过。
- 在推送路由里勾选多个渠道，插件通知就会同时发送到这些地方。

**双向同步**
- 在通知渠道配置里选择插件，推送路由会自动更新。
- 在推送路由里选择渠道，通知渠道配置也会自动更新。
- 删除渠道时，所有插件的推送路由会自动清理该渠道，不会留下无效配置。

**界面优化**
- 设置界面更宽（从 760px 增加到 1200px），在宽屏显示器上更舒适。
- 通知渠道卡片间距更大，排列更美观。
- 添加渠道的下拉菜单改为从右下方展开，更符合操作习惯。
- 编辑渠道时不再显示类型字段，界面更简洁（类型创建时已选择，不可更改）。

**旧配置自动迁移**
- 旧版本的 Bot 配置会自动迁移到新的通知渠道格式。
- 迁移后保持所有 Bot 的 ID 不变，推送路由不会断开。
- 后端继续兼容旧配置格式，升级无缝过渡。

## v1.1.0.8

这一版优化了插件市场的筛选功能位置，并完善了通知渠道的配置选项。

- 插件市场的筛选功能移到了右上角独立按钮，点击后弹出筛选弹窗，不再嵌在搜索框里，更容易找到。
- 筛选弹窗支持按作者、仓库筛选，以及多种排序方式（热门、插件名称、作者、插件仓库、最新发布）。
- 通知渠道配置完善了各渠道的详细参数，包括 Telegram 的用户白名单、管理员白名单、代理API，企业微信的 Token、EncodingAESKey 等。

## v1.1.0.7

这一版重构了通知渠道配置界面，并新增了两种新的通知方式。

- 通知渠道配置界面改为卡片展示加弹窗编辑的形式，每个渠道的状态和配置一目了然，操作更直观。
- 新增 Bark 通知渠道，可以把平台和插件的通知推送到你的 iPhone 或 iPad。
- 新增企业微信应用通知渠道，可以通过企业微信收到平台消息。
- 移除了插件市场的标签筛选功能，简化界面操作。

## v1.1.0.6

这一版大幅增强了插件市场的搜索和筛选能力，方便快速找到需要的插件。

- 搜索时可以按"热门、插件名称、作者、插件仓库、最新发布"五种方式排序，热门排序会优先显示可更新的插件。
- 新增按作者筛选，可以快速查看某个作者发布的所有插件。
- 新增按仓库筛选，可以只看来自特定插件仓库的插件。
- 所有筛选条件可以组合使用，比如"只看已安装 + 某个作者"。
- 搜索界面的排序和筛选区域采用垂直布局，选项一目了然，选中项用蓝色背景标识。
- 修复了插件图标左下角有时会显示占位符图案的问题。

## v1.1.0.5

这一版让多个通知 Bot 更方便管理，新增、切换和修改后不用重启整个平台。

- 内置 Bot 的名称现在可以修改，多个 Bot 中的任意一个都可以设为默认 Bot。
- 新增、删除、改名、更换 Token 或切换默认 Bot 后，保存设置就会立即生效。
- 更换 Token 时会先确认新 Bot 能正常连接，再停用旧连接；如果新 Token 有问题，原来的 Bot 会继续工作。
- 删除当前默认 Bot 时会自动切换到仍然可用的 Bot，不会让插件通知突然失去发送目标。
- 插件可以跟随当前默认 Bot，也可以单独指定内置 Bot 或其他 Bot，通知发送和插件运行会使用同一个选择。
- 通知设置中的提示和状态文字已经同步调整，可以直接看出哪些改动已生效、哪些 Bot 连接失败。

## v1.1.0.4

这一版让插件常用信息和通知设置更容易找到，也改善了不同形状插件图标的显示效果。

- 插件配置弹窗新增“通知 Bot”选择，可以直接指定这个插件使用哪个 Bot，切换后立即生效，并与系统通知页面保持一致。
- 插件菜单中的“查看历史”改名为“版本历史”，名称与实际内容更贴切。
- 插件菜单新增“项目主页”，可以直接打开该插件对应的 GitHub 仓库；没有仓库来源的插件不会显示错误入口。
- 横向、圆形和透明背景的插件 Logo 现在会按原比例完整显示，不再为了填满方框而裁掉部分内容。

## v1.1.0.3

这一版修复了插件页面几个容易看着别扭的显示问题，让图标和搜索界面更清楚。

- 没有单独图标的插件只会显示平台默认图标，不再叠着一个多余的图片占位标志。
- “官方”标识换成更像认证徽章的图标，不再只是一个普通对勾。
- 修复插件搜索中的筛选栏被插件列表压扁的问题，“全部、已安装、可更新、可安装”现在可以完整显示。
- 手机上仍然可以左右滑动搜索筛选项，同时隐藏了多余的滚动条。

## v1.1.0.2

这一版解决了插件历史日志在重启或更新后消失的问题，并让日志清理更方便管理。

- 插件历史日志会保存在日志目录中，重启平台或更新版本后仍能继续查看。
- 日志记录会同时显示日期和时间，查看以前的运行情况更清楚。
- 系统设置的“维护”页面新增日志清理设置，可以选择是否启用、每天几点执行以及保留多少条记录。
- 定时清理会同时处理平台运行日志和插件历史日志，清理过的旧记录不会在重启后重新出现。
- 只修改日志清理设置时会立即生效，不再提示需要重启平台。

## v1.1.0.1

这一版全面美化了插件页面的视觉效果，让界面更美观、交互更流畅。

- 插件卡片鼠标悬停时会轻微上浮并显示阴影，点击时有缩放反馈，视觉效果更生动。
- 所有主按钮（如"上传插件""刷新市场""保存"）改用蓝色渐变背景，更醒目也更统一。
- 出现错误的插件卡片左侧会显示红色竖线标识，一眼就能看出哪些插件有问题。
- 可更新插件的区域会用淡黄色背景和左侧标识线突出显示，不会错过更新提示。
- "官方"徽章前面加上勾选图标，更直观地表示官方认证。
- 版本更新徽章带有轻微的脉冲动画，提醒用户注意更新。
- 插件卡片右上角的三点菜单改用更现代的SVG图标，下拉菜单带有滑入动画。
- 所有警告和错误提示前面都会显示对应的图标（三角形警告或圆形感叹号），更容易识别。
- 右下角的搜索悬浮按钮鼠标悬停时会显示"搜索插件 (Ctrl+K)"提示。
- 所有按钮、标签页和卡片使用键盘导航时会显示蓝色轮廓，方便键盘用户操作。
- 插件网格布局在大屏幕上限制最大宽度，避免卡片过宽；手机上自动调整为单列显示。
- 移除了"舒适/紧凑"显示密度切换按钮，统一使用舒适模式显示，界面更简洁。

## v1.1.0

这一版修复了几个影响使用的问题，让插件更新内容更容易查看，也让系统状态首页更直观。

- 修复备份恢复功能的路径检查错误，之前会错误地拒绝恢复备份包中的顶层文件，现在可以正常恢复所有文件。
- 修复备份恢复后系统会重复执行恢复操作的问题。现在恢复成功后会自动清理临时文件，避免每次启动都重复恢复。
- 插件作者填写更新说明后，用户可以从插件卡片右下角的三点菜单打开“查看历史”，直接了解这个版本改了什么。
- 系统状态首页加入更自然的动态效果。页面加载时会平滑显示内容，账号数量、插件数量和活跃次数发生变化时也不会突然跳动。
- 插件活跃图现在可以查看每个时间段的详细次数，指向图例时会突出对应插件；账号状态或定时任务发生变化时，页面会短暂标出变化位置。
- 添加插件仓库时会统一显示为“用户名/AWBotNest-Plugins”，其他仓库名不会被加入；保存成功、重复添加或填写了内置官方仓库时，页面都会明确告诉用户结果。

## v1.0.9

这一版主要让平台更容易看懂、备份和维护。

- 运行日志现在优先显示插件的中文名称。以前看到的通常是英文代号，现在一眼就能知道是哪个插件在运行。
- 一批难懂的报错和提示已经改成白话，例如直接告诉你“端口可能被占用”或“用户账号连接失败”，不用再猜英文报错是什么意思。
- 系统设置新增“维护”页面，可以下载整个平台的备份，也可以导入以前的备份。导入时会先检查备份是否完整，并自动下载一份当前数据作为退路；真正恢复会在重启平台时进行，避免损坏正在使用的数据。
- 账号管理页面增加更清楚的状态提示，现在能区分“在线”“未登录”“已手动下线”和普通离线，也会告诉你这个账号是否已经完成过登录。
- 插件的设置页面可以更灵活地安排输入项的顺序和宽度，内容较多时不再显得杂乱。
- 插件仓库设置变得更简单，只需要填写公开仓库地址。自动检查时间由平台管理，不再需要手动填写；私有仓库也不再提供入口。
- 插件页面右下角新增搜索按钮。插件多时可以按名称、作者、说明或英文标识快速查找，并直接打开设置、安装或更新。
- 网页整体样式更加统一，系统状态是否正常会直接显示在页面顶部。插件可以按状态筛选并切换卡片大小，点击卡片还能查看完整说明、安装状态和常用操作；手机上的弹窗和底部按钮也更方便使用。

## v1.0.8

这一版让复杂插件可以拥有自己的设置页面，也让插件具备打开网页并读取内容的能力。

- 复杂插件可以自带专属设置页面，用来展示图表、运行状态和更丰富的操作。普通插件仍然使用原来的设置表单，不受影响。
- 专属设置页面可以安全地读取插件数据或执行操作，只有已经登录的管理员才能使用。
- 插件的页面文件也受到登录保护，未登录的人无法直接打开。
- 平台新增网页访问能力。插件需要读取动态网页或遇到网站访问限制时，可以直接使用，不必由每个插件重复安装浏览器。相关文件只会在第一次使用时下载并保存，不使用这项功能就不会额外占用空间。
- 新增一份专属设置页面的示例插件，插件作者可以照着修改。

## v1.0.7

这一版主要改善插件设置页面，让管理员填写配置更方便。

- 新增“列表”输入项，可以一行行添加或删除规则，不用把多条内容挤在一个输入框里。
- 新增“会话选择”输入项，需要选择群组、频道或私聊时可以直接从列表中挑选，不必到处查找很长的会话编号。账号尚未登录时也可以手动填写。
- 新增“操作按钮”，插件可以在设置页面放置“测试连接”“立即运行”等按钮，点击后马上显示结果。
- 新增“说明和状态”区域，可以显示使用提示、上次运行时间或已经处理的数量。
- 保存设置时会检查必填内容和数字范围，填写不完整或数字不合适时会直接指出具体位置。
- 插件可以保存自己的运行状态，也可以更方便地下载消息中的图片和文件。
- 修复会话尚未选择却显示数字 0，以及部分必填内容没有被正确检查的问题。

## v1.0.6

这一版新增“外部通知入口”（Webhook），其他程序可以通过专属地址向平台或插件发送消息。

- 外部程序可以向平台发送内容，再由平台把它作为通知推送给管理员。
- 插件也可以拥有自己的接收地址。外部事件到达后，插件可以处理内容并返回结果。
- 所有接收地址共用一把安全密钥，可以在“系统设置 → 通知”中一键生成。没有生成密钥时，这项功能不会开启。
- 插件停用或重新加载后，旧的接收地址会立即失效，避免消息继续送到已经停止的插件。

## v1.0.5

这一版支持同时使用多个通知机器人。

- 可以在“系统设置 → 通知”中添加一个默认机器人和多个额外机器人，并为每个插件选择由哪个机器人发送通知。没有单独选择的插件仍使用默认机器人，原有设置不用修改。
- 插件的通知和主动发送的消息都会使用所选机器人。
- 删除机器人后，原来使用它的插件会自动改回默认机器人。
- 添加或删除机器人后需要重启平台，切换插件使用的机器人则会立即生效。
- 机器人的登录密钥统一放到“通知”页面管理，不再分散在其他设置中。
- 页面中的“主人”和“所有者”统一改为“管理员”，称呼更清楚。

## v1.0.4

这一版让插件访问网络时自动使用平台设置的代理。

- 开启代理后，插件访问外部网站会自动经过代理，不需要给每个插件单独设置。本机上的服务仍会直接连接。
- 修改代理后，新启动或重新加载的插件会立即使用新设置；已经建立的长期连接需要重启平台后才会切换。
- Telegram 连接、安装插件所需组件、智能助手和插件市场现在共用同一套代理设置，行为更一致。

## v1.0.3

- 修复多个插件同时接收同一类消息时互相抢消息的问题。现在每个相关插件都能收到消息。
- 插件仍然可以主动拦下一条消息，让后面的插件不再重复处理。
- 插件可以显示自己的图标，不再全部使用平台标志。
- 修复只选择一个账号，其他账号却也跟着执行的问题。插件现在会严格按照选中的账号运行。
- 插件从市场更新后，如果正在运行会自动重新加载；没有运行的插件只更新文件，不会被自动开启。
- 修复刷新插件市场后仍看不到新版本的问题，现在刷新页面就能获取最新内容。
- 取消单个插件文件原有的 8MB 限制，带有较大资源文件的插件也能安装。
- 插件需要额外组件时，平台会在启用插件时自动检查和安装。已经安装的不会重复处理，版本不兼容时会说明原因并停止启用。
- 安装额外组件时也会使用平台代理，并在网络不可用时尽快提示失败，不再长时间卡住其他插件。
- 代理设置页面新增“下载来源”选项，默认使用国内速度较快的清华镜像；留空时使用官方来源。
- 插件额外安装的组件会保存在数据目录中，更新容器或镜像后不用重新安装。

## v1.0.2

- 插件市场和智能助手访问网络时会使用平台代理，受限网络环境下也能正常使用。
- 侧边栏新增“重启平台”按钮，修改重要设置后可以直接点击重启。
- 手机浏览器可以把平台添加到主屏幕，像普通应用一样打开。手机页面也改成更适合小屏幕的底部菜单和卡片布局。
- 网页图标改为平台标志。没有填写账号信息时，只显示一条清楚的提醒，不再连续刷出报错。
- 活跃统计只计算插件真正执行的发送、回复和编辑操作，空闲时不会再增加。插件较多时图表也会自动整理。
- 修复插件启用状态偶尔自己关闭的问题。加载失败只会显示错误，不会把插件当成手动停用。
- 插件卡片上的来源地址改为简短形式，更容易阅读。
- 登录页底部显示版权和版本号。默认账号密码只写入后台日志，不再直接展示在登录页面。
- 修复重启平台或更新镜像后，原本启用的插件全部变成未启用的问题。
- 修复通知机器人没有连接成功时，插件看似开启却收不到消息的问题。
- 修复自动更新误删本地上传或自己修改的同名插件。现在只会更新确实从插件市场安装的内容。
- 安装包含多个文件的插件时，会在全部文件下载成功后再正式替换；中途失败不会留下残缺文件。
- 加强插件下载和登录安全，防止伪造下载地址、超大文件和危险文件路径。
- 首次使用默认密码登录后，必须先设置自己的密码，才能上传插件、导入内容或修改重要设置，避免平台被默认密码接管。

## v1.0.1

- 项目中的表情符号全部改为文字，说明、日志和通知的显示更统一。
- 通知改用“通知、成功、警告、错误”等文字标记，不再依赖图标区分。
- 运行日志只保留时间、级别和内容，去掉不常用的文件名和来源栏目；插件日志仍会标明是哪个插件发出的。

## v1.0.0

首个正式版本。

- 支持安装单文件插件，一个插件对应一个功能。启用或停用插件时不需要重启平台。
- 内置插件市场，也可以添加其他插件仓库，浏览并一键安装需要的插件。安装后不会自动开启，由管理员自己决定。
- 可以在网页中登录和管理多个 Telegram 账号，并为每个插件选择在哪些账号上运行。
- 网页控制台集中提供插件管理、账号管理、运行日志、系统状态和系统设置。
- 插件产生的通知由平台统一整理，标明来自哪个账号后再发送给管理员。
- 网页控制台使用用户名和密码登录，密码会经过加密保存。
