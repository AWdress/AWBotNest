# Cron 编辑器设计核对

**视觉依据**

- 源图：`docs/design-qa/cron-reference.png`
- 最终对照截图：`docs/design-qa/cron-comparison.jpg`
- 源图像素：757 × 204；实现截图像素：820 × 620
- CSS 视口：820 × 620；设备像素密度：1；源图与实现主体均按 757 CSS px 宽度显示，无密度缩放
- 状态：深色主题、Cron 值 `6 3 * * *`、输入框聚焦、可视化规则面板展开

**Findings**

- 无剩余 P0、P1 或 P2 问题。
- [可接受差异] 实现使用 AWBotNest 的深蓝背景 token，源图背景略偏中性；交互层级、焦点强调和绿色规则胶囊保持一致，未引入脱离平台主题的独立配色系统。

**五项视觉核对**

- 字体与排版：表达式使用等宽数字字体；正文、胶囊和标签的字号、字重及单行节奏与源图一致，中文没有截断。
- 间距与布局：输入框约 68px 高，圆角 14px；展开面板约 100px 高，并从半宽输入框下方扩展到整行；各规则段落与冒号间距一致。
- 色彩与 token：焦点使用现有 V2 紫色 token，规则值使用与源图接近的绿色；文字、边框和暗色表面对比清晰。
- 图标与素材：清除操作使用项目现有 Lucide `X` 图标；源图没有需要补充的品牌、照片或插画素材。
- 文案与内容：`每 年 的 每月 的 每日 和 一周的每一天 的 03 × : 06 ×` 与源图一致。

**交互与响应式证据**

- 在 Codex 内置浏览器中实际打开组件，小时由 `03` 改为 `08` 后，表达式同步为 `6 8 * * *`。
- 窄屏状态下规则胶囊会自然换行，不产生横向溢出；5 位和 6 位表达式均保留直接输入能力。
- 浏览器控制台 warning/error：0。

**比较历史**

1. 初次实现存在 P2 差异：输入框和面板高度偏小、焦点为蓝色、清除按钮是空心图标。
2. 修复：输入框调整到 68px 与 14px 圆角，面板调整到约 100px，焦点切换到 V2 紫色，清除按钮改为源图式实心圆底；规则面板扩展为配置栅格整行宽度。
3. 修复后证据：`docs/design-qa/cron-comparison.jpg` 上半部分为源图，下半部分为同宽实现，未发现新的 P0/P1/P2 差异。

**Implementation Checklist**

- [x] Schema 与 Vue 共用同一平台组件
- [x] 规则胶囊可编辑并实时回写 Cron 字符串
- [x] 复杂表达式可原样输入并显示明确状态
- [x] 手机端可换行且保持 44px 以上触控区域
- [x] 前后端统一校验 5 位和 6 位 Cron

**Follow-up Polish**

- 无阻塞项。

final result: passed
